package com.example.ecommerceaiagent.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════
// 极简现代 · 克制配色
// 参考 ChatGPT / Telegram 设计语言
// ═══════════════════════════════════════════════

// Accent — 唯一强调色
val Accent = Color(0xFF6366F1)       // Indigo

// Surface
val Background = Color(0xFFF7F7F8)   // 极浅灰 (ChatGPT style)
val Surface = Color(0xFFFFFFFF)       // 纯白
val CardBg = Color(0xFFF9FAFB)

// Text
val TextPrimary = Color(0xFF1A1A1A)
val TextSecondary = Color(0xFF666666)
val TextHint = Color(0xFF999999)

// Functional
val Divider = Color(0xFFE5E5E5)
val InputBg = Color(0xFFF2F2F2)
val UserBubble = Color(0xFF2D2D2D)    // 深灰 (Telegram/ChatGPT user bubble)
val AiBubble = Color(0xFFFFFFFF)

// 兼容旧引用
val Primary = Accent
val PrimaryLight = Color(0xFF818CF8)
val PrimaryDark = Color(0xFF4F46E5)
val PrimaryContainer = Color(0xFFEEF2FF)
